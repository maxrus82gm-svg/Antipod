# История_задач

# TASK 001 — Создать стартовый агентский цикл

Статус:
Готово.

Сделано:
- Создан `START.md`.
- Создан `Documentation/AGENT_TASK_LOOP.md`.
- Создан `Documentation/TASK_HISTORY.md`.
- Зафиксированы обязательные документы для чтения перед началом работы.
- Зафиксированы правила отчета, review / status и очистки активной задачи.

Изменены файлы:
- `START.md`
- `Documentation/AGENT_TASK_LOOP.md`
- `Documentation/TASK_HISTORY.md`
- `Progekt_antipod/Antipod/.obsidian/workspace.json` отображается измененным в git status из-за обновления состояния Obsidian; агент вручную его не редактировал.

Проверка:
- Проверена структура проекта.
- Проверено отсутствие обязательных документов до создания.
- Проверены байты существующего Markdown-файла с кириллицей: файл хранится в UTF-8.

Проблемы:
- Консоль PowerShell отображает кириллицу как mojibake, хотя байты файла корректны.
- Тематические правила проекта пока не созданы.
- Obsidian обновил состояние `.obsidian/workspace.json` после создания новых документов.

Итог:
- Базовый цикл готов. Следующая задача может быть добавлена в активный блок `Documentation/AGENT_TASK_LOOP.md`.

# A-002 — Вынести правила создания новых документов в отдельный документ

Статус:
Готово.

Сделано:
- Создан `06_Правила_создания_документов.md`.
- Создан `Documentation/Список_правил.md`.
- В `Старт.md` добавлено правило, когда читать этот документ.
- Ветка правил построена от `Documentation/Задача_агенту.md` через `Documentation/Список_правил.md`.
- Сохранена форма графа: правила не подключены напрямую к `00_Главная.md`.

Изменены файлы:
- `06_Правила_создания_документов.md`
- `00_Главная.md`
- `Старт.md`
- `Documentation/Задача_агенту.md`
- `Documentation/Список_правил.md`
- `Documentation/История_задач.md`

Проверка:
- Проверен список файлов.
- Проверены Obsidian-ссылки.
- Проверена кодировка Markdown-файлов: UTF-8 без BOM.

Проблемы:
- В задаче указан путь `00_Главная/06_...md`, но в проекте `00_Главная.md` уже используется как центральный документ. Поэтому новый документ создан рядом с ним: `06_Правила_создания_документов.md`.

Итог:
- Правила создания новых документов вынесены в отдельный файл и доступны через ветку правил агента.

# A-003 — Создать правила Git и Review для проекта Antipod

Статус:
Готово.

Сделано:
- Создан `07_Правила_Git_и_Review.md`.
- В `Старт.md` добавлено короткое правило Git / Review.
- В `Documentation/Список_правил.md` добавлена ссылка на новый документ правил.
- Закреплено правило: Git полностью контролирует пользователь.
- Закреплены запрещённые для агента Git-действия, которые меняют состояние репозитория.

Изменены файлы:
- `07_Правила_Git_и_Review.md`
- `Старт.md`
- `Documentation/Список_правил.md`
- `Documentation/Задача_агенту.md`
- `Documentation/История_задач.md`

Проверка:
- Проверен список файлов.
- Проверены Obsidian-ссылки.
- Проверена кодировка Markdown-файлов: UTF-8 без BOM.
- Выполнен безопасный просмотр `git status --short --untracked-files=all`.

Git:
- Commit не создавался агентом.
- Push не выполнялся агентом.
- Git-история не изменялась агентом.

Проблемы:
- В задаче указан путь `00_Главная/07_...md`, но в проекте `00_Главная.md` уже используется как центральный документ. Поэтому новый документ создан рядом с ним: `07_Правила_Git_и_Review.md`.

Итог:
- Правила Git и Review вынесены в отдельный файл и доступны через ветку правил агента.

# A-004 — Добавить в Старт правило GitHub-review после отчёта агента

Статус:
Готово.

Сделано:
- В `Старт.md` добавлено короткое правило GitHub-review после отчёта агента.
- Закреплено, что при REVIEW или сообщении о выполненной задаче ассистент сам проверяет GitHub по правилам `07_Правила_Git_и_Review.md`.
- Закреплено, что ассистент не должен опираться только на текстовый отчёт агента.
- Полный текст правил Git / Review не дублировался в `Старт.md`.

Изменены файлы:
- `Старт.md`
- `Documentation/Задача_агенту.md`
- `Documentation/История_задач.md`

Проверка:
- Проверены Obsidian-ссылки.
- Проверена кодировка Markdown-файлов: UTF-8 без BOM.
- Выполнен безопасный просмотр `git status --short --untracked-files=all`.

Git:
- Commit не создавался агентом.
- Push не выполнялся агентом.
- Git-история не изменялась агентом.

Проблемы:
- Консоль PowerShell некорректно отображает кириллицу, поэтому кодировка проверялась байтами.

Итог:
- Стартовые правила теперь требуют GitHub-review по правилам Git / Review после отчёта агента или при REVIEW результата.

# A-005 — Закрепить правило большого и малого блока для задач агента

Статус:
Готово.

Сделано:
- `Documentation/Агентский_цикл.md` переименован в `Documentation/Задача_агенту.md`.
- Обновлены ссылки на документ задачи агенту.
- Закреплено правило большого и малого блока.
- Закреплено, что уточнения после подготовки большого блока добавляются в малый блок.
- Укорочен формат отчёта агента.
- Закреплено, что последний отчёт хранится в `Documentation/Задача_агенту.md` ниже активной задачи.

Изменены файлы:
- `Старт.md`
- `00_Главная.md`
- `Documentation/Задача_агенту.md`
- `Documentation/Список_правил.md`
- `Documentation/История_задач.md`
- `06_Правила_создания_документов.md`
- `.obsidian/graph.json`
- `.obsidian/workspace.json`

Проверка:
- Проверены ссылки на старое имя `Агентский_цикл`.
- Проверены Obsidian-ссылки.
- Обновлён цветовой запрос графа Obsidian на новое имя документа.
- Проверена кодировка Markdown-файлов: UTF-8 без BOM.
- Выполнен безопасный просмотр `git status --short --untracked-files=all`.

Git:
- Commit не создавался агентом.
- Push не выполнялся агентом.
- Git-история не изменялась агентом.

Проблемы:
- Консоль PowerShell некорректно отображает кириллицу, поэтому кодировка проверялась байтами.

Итог:
- Правило большого и малого блока закреплено, а документ задачи агенту получил понятное имя.

# A-006 — Очистить активный БЛОК 1 и добавить ignore для Obsidian workspace

Статус:
Готово.

Сделано:
- Очищен активный `БЛОК 1` в `Documentation/Задача_агенту.md`.
- Создан `.gitignore` в корне Obsidian-проекта.
- В `.gitignore` добавлены только `.obsidian/workspace.json` и `.obsidian/workspace-mobile.json`.
- В `Documentation/Задача_агенту.md` записан компактный отчёт A-006.

Изменены файлы:
- `Documentation/Задача_агенту.md`
- `.gitignore`
- `Documentation/История_задач.md`

Проверка:
- Проверено, что `БЛОК 1` очищен.
- Проверено, что `.gitignore` не игнорирует всю папку `.obsidian`.
- Проверено, что `Progekt_antipod/Antipod/.obsidian/workspace.json` уже отслеживается Git.
- Проверена кодировка Markdown-файлов: UTF-8 без BOM.

Git:
- Commit не создавался агентом.
- Push не выполнялся агентом.
- Pull не выполнялся агентом.
- Ветки не создавались и не переключались агентом.
- `git rm` не выполнялся агентом.
- Git-история не изменялась агентом.

Проблемы:
- `.obsidian/workspace.json` уже отслеживается Git; исключение из отслеживания должен выполнить пользователь вручную, если это потребуется.

Итог:
- Активный блок очищен, workspace-файлы добавлены в ignore для будущих изменений.

# A-007 — Закрепить правило полного списка фактических изменений в отчёте агента

Статус:
Готово.

Сделано:
- Закреплено правило полного списка фактических изменений в отчёте агента.
- Обновлён компактный формат отчёта агента.
- Добавлены разделы `UNEXPECTED CHANGES` и `UNTRACKED`.
- В Git/Review-правила добавлено короткое уточнение про отражение всех фактических изменений.

Изменены файлы:
- `Documentation/Задача_агенту.md`
- `07_Правила_Git_и_Review.md`
- `Documentation/История_задач.md`

Проверка:
- Проверено, что новый формат разделяет `CHANGED`, `UNEXPECTED CHANGES` и `UNTRACKED`.
- Проверено, что правило требует указывать побочные изменения из безопасных Git-проверок чтения.
- Проверено, что `git diff --check` сообщает о хвостовых пробелах в большом блоке A-007.
- Проверено, что Git-команды использовались только для чтения.

Побочные изменения:
- `.obsidian/graph.json` — изменился вне намеренных правок A-007.
- `.obsidian/workspace.json` — изменился вне намеренных правок A-007.

Git:
- Commit не создавался агентом.
- Push не выполнялся агентом.
- Pull не выполнялся агентом.
- Ветки не создавались и не переключались агентом.
- `git rm` не выполнялся агентом.
- Git-история не изменялась агентом.

Проблемы:
- `.obsidian/graph.json` и `.obsidian/workspace.json` видны как изменённые в `git status`; агент не исправлял их Git-командами.
- `git diff --check` сообщает о хвостовых пробелах в большом блоке A-007; агент не чистил их, чтобы не расширять задачу.

Итог:
- Правило отчёта теперь требует показывать все фактические изменения, включая побочные и untracked.
